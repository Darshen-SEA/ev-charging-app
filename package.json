{
  "name": "ev-charging-app",
  "version": "1.0.0",
  "description": "EV Charging Station Finder - Serverless Backend",
  "main": "src/handler.js",
  "scripts": {
    "deploy": "npm run build && serverless deploy",
    "deploy:frontend": "cd ClientApp && npm install && npm run build && cd .. && serverless deploy",
    "offline": "serverless offline start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "serverless-offline": "^12.0.4",
    "serverless-s3-sync": "^3.1.0"
  }
}
